NOT DEPLOYED — run locally using docs/setup-guide.md

TrialGuard is a local proof-of-concept (synthetic data only). After setup, start it with:
  .venv\Scripts\python.exe -m streamlit run src\app.py
and open http://localhost:8501 on the same computer.
